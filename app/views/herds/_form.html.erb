<%= form_with(model: herd, local: true) do |form| %>
  <% if herd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(herd.errors.count, "error") %> prohibited this herd from being saved:</h2>

      <ul>
      <% herd.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
<%= form.label "Enter Pasture ID" %>
  <%= form.collection_select(:pasture_id, current_user.pastures, :id, :id, {prompt: "Enter Pasture"}, {class: "w3-select"}) %> 
  
</div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
