<h1 class="text-center">
<p>New Animal</p>
</h1>

<%= render 'form', animal: @animal %>

<%= link_to 'Back', animals_path(user: current_user, tag_number: @animal.tag_number),
                                      class: "btn btn-xs btn-success", method: :post %>
